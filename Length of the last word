class Solution {
    public int lengthOfLastWord(String s) {
        String str[] = s.split(" ");
        int n = str.length,count=0;
        String l = str[n-1];
        for(char ch : l.toCharArray()){
            count++;
        }
        return count;
    }
}
